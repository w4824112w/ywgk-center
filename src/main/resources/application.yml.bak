spring:
    application:
        name: ywgk-center

server:
    port: 8083 #启动端口

eureka:
  client:
    registerWithEureka: false  #false:不作为一个客户端注册到注册中心
    fetchRegistry: false      #false:它是用来管理服务的，所有不需要检索服务
